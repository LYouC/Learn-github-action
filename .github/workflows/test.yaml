name: test docker action
on: [push]

jobs:
    build:
        runs-on: ubuntu-24.04
        steps:
          - uses: actions/checkout@v4
          - name: Build the Docker image
            run: ls -la
